import 'package:flutter/material.dart';
class AggregateView extends StatelessWidget {
  const AggregateView({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Aggregate — TODO')));
}
